export type StoryScene = {
  id: string;
  title: string;
  caption: string;
  callouts: string[];
  sources: string[];
};

export type SourceLink = {
  id: string;
  title: string;
  url: string;
};
